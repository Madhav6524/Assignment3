# Assignment3

# task1   find factorial

- first i take user input and convert in to integer and store in x
- create function name factorial with one parameter
- result=1
- for loop 1 to x+1 . that runes total x times.
- result=result*i
- function return the result
-  print the result


  # task2  use of math module

  - first import math module
  - take a number using input() and convert into integer and store in x
  - use math.sqrt() to find squre root and display the result
  - use math.sin() to fing=d sin and display the result
  - use math.log() to find logathim and display the result
